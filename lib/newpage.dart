import 'package:flutter/material.dart';

class Ashik extends StatelessWidget {
  const Ashik({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Done"),
      ),
    );
  }
}
